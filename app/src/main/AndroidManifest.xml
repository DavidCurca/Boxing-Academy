<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wow.boxingacademy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pba_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pba_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShortActivity"></activity>

        <activity android:name=".openVideoActivity" />

        <activity
            android:name=".openPhotoActivity"
            android:configChanges="keyboard|keyboardHidden|orientation" />
        <activity android:name=".GallleryActivity" />
        <activity android:name=".NewReviewActivity" />
        <activity android:name=".RecenziiActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8713196903791104~9804159712" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>